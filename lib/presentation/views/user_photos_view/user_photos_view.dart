import 'package:flutter/material.dart';

class UserPhotosView extends StatelessWidget {
  const UserPhotosView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('User photos view'),
    );
  }
}
