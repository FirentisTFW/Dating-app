import 'package:flutter/material.dart';

class ProfileCreationView extends StatelessWidget {
  const ProfileCreationView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Profile creation view'),
      ),
    );
  }
}
